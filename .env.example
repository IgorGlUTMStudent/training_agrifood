# Optional. Empty uses same-origin `/api` requests and the Vite dev proxy.
VITE_API_BASE_URL=
# Example direct API origin: http://localhost:8000

# Comma-separated origins accepted by the backend during local development.
SMART_HARVEST_CORS_ORIGINS=http://localhost:5173
